Installing Sfark-Extractor
=================

Requirements:

The following dependencies are required:

    Python 3.4
    https://github.com/raboof/sfArkLib
    https://github.com/raboof/sfarkxtc (need root installation: sudo make
install

Installation:
 - On UBUNTU (16.04 and above) and DEBIAN (8 and above) all the dependencies should be in the official repositories
 sudo apt-get install sfarkxtc python3-tk

 - On other LINUX system (on Mac/OSX, and BSD too) please install the dependencies. sfarkxtc must be installed in ROOT!

Usage:

Run ./sfark-extractor
